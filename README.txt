
Depends: e1071 (>= 1.6-7)

==================INSTALL (on Mac’s)================================

to install, open terminal and type:
R

then type:
install.packages(<path_to_package>, repos = NULL, type="source")

p.s. this <pathtopackage> must be in “”

load the package by typing

library("KLHMM”)

p.p.s. type ?KLHMM to see description

=================EXAMPLE===========================================

CODE:

Look at Example.R — an example code how to use KLHMM package

===================================================================
JZ 
